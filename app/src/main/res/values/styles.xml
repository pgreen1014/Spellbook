<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppToolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="ClericTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/cleric_colorPrimary</item>
        <item name="colorPrimaryDark">@color/cleric_colorPrimaryDark</item>
        <item name="colorAccent">@color/cleric_colorAccent</item>
    </style>

    <style name="DefaultTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/all_spells_colorPrimary</item>
        <item name="colorPrimaryDark">@color/all_spells_colorPrimaryDark</item>
        <item name="colorAccent">@color/all_spells_colorAccent</item>
    </style>

</resources>
